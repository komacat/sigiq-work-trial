import type { Instruction } from '@/lib/types'

export const script: Instruction[] = [
    {
        interaction: 'point',
        payload: {
            elementId: 'main-text',
        },
    },
    {
        interaction: 'point',
        payload: {
            elementId: 'main-text',
        },
    },
]
